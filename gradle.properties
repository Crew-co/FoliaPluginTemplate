# ----------------------------------------------------------------------------
# Project coordinates — change these to make the template your own.
# ----------------------------------------------------------------------------
group=com.crewco
version=1.0.0

# ----------------------------------------------------------------------------
# Folia API version. Matches the Minecraft version you target.
# Published on the PaperMC repo as dev.folia:folia-api:<this>.
# This IS read by build.gradle.kts.
# ----------------------------------------------------------------------------
foliaApiVersion=1.21.11-R0.1-SNAPSHOT

# ----------------------------------------------------------------------------
# Gradle daemon niceties
# ----------------------------------------------------------------------------
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true

# Note: plugin versions (Kotlin 2.1.0, Shadow 8.3.11, plugin-yml 0.6.0,
# run-paper 3.0.2) live in the plugins { } block of build.gradle.kts, not here.
